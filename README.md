# 🧾 Student API Server

## 📘 프로젝트 개요
이 프로젝트는 **Flask + SQLAlchemy** 기반의 RESTful API 서버로,  
학생(Student) 정보를 관리합니다.  
CRUD 기능(생성, 조회, 수정, 삭제)과 **공통 응답 포맷**, **에러 처리**,  
**미들웨어(요청 로그)** 기능을 포함합니다.

---

## ⚙️ 주요 기능

| 분류 | 엔드포인트 | 설명 | 
|------|-------------|------|
| **POST** | `/students` | 학생 생성 | 
| **POST** | `/students/<id>/activate` | 학생 활성화 | 
| **POST** | `/students/<id>/deactivate` | 학생 비활성화 | 
| **GET** | `/students` | 전체 학생 조회 |
| **GET** | `/students/<id>` | 특정 학생 조회 | 
| **PUT** | `/students/<id>` | 학생 정보 수정 |
| **PUT** | `/students/<id>/grade` | 성적만 수정 | 
| **DELETE** | `/students/<id>` | 특정 학생 삭제 | 
| **DELETE** | `/students/inactive` | 비활성 학생 전체 삭제 | 
| **GET** | `/students/db-error` | 서버 일시적 오류 테스트 |
| **GET** | `/students/error` | 서버 특정 오류 (여기서는 예시로 강제로 0으로 나눠서 예시로만 사용) | 

---

## 🧩 공통 응답 포맷

모든 API는 `make_response()` 함수를 통해  
다음과 같은 **일관된 JSON 포맷**으로 응답합니다:

```json
{
  "status": "success | fail | error",
  "message": "optional, 설명 메세지",
  "data": "optional, 응답 데이터"
}
``` 


status: 요청 결과 상태 (success, fail, error)  
message: 오류나 추가 안내가 필요한 경우 포함  
data: 요청 결과 데이터 (예: 학생 정보)  

모든 응답은 status 필드를 공통으로 포함합니다.  
message와 data는 상황에 따라 생략될 수 있습니다.  

---

🧱 **미들웨어 (Middleware)**  
Flask의 `@app.before_request` 데코레이터를 사용하여  
모든 요청의 정보를 콘솔에 출력하는  
로깅 미들웨어를 구현했습니다.  

Postman에서 요청을 전송할 때,  
Flask 서버 터미널에 로그가 자동 출력됩니다.  

---

🚨 **에러 코드 정리**

| 코드 | 의미 | 처리 핸들러 | 예시 메시지 |
|------|------|--------------|--------------|
| **400** | 잘못된 요청 | `bad_request()` | `"Bad Request"` |
| **404** | 리소스 없음 | `not_found()` | `"Resource Not Found"` |
| **409** | 데이터 충돌 | `create_student()` 내 처리 | `"Student with this name already exists"` |
| **500** | 서버 내부 오류 | `internal_error()` | `"Internal Server Error"` (예시: 0으로 나눔) |
| **503** | 서비스 일시 중단 | `db_error_demo()` | `"Service temporarily unavailable"` |

---

🧠 **요약**

✅ Flask + SQLAlchemy 기반 RESTful 서버  
✅ CRUD, 에러 처리, 공통 응답 포맷, 미들웨어 완비  
✅ 다양한 HTTP 상태 코드 (2xx / 4xx / 5xx)  
✅ Postman으로 전체 테스트 완료
